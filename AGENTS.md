# AGENTS.md

## Cursor Cloud specific instructions

This is currently a blank starter repository (`cursor-test`) with no application code, dependencies, or services. The repo contains only a `README.md`.

- **No build system, linter, test framework, or dev server** is configured.
- **No dependencies** to install (no `package.json`, `requirements.txt`, `Cargo.toml`, etc.).
- **No services** to run.

When application code is added in the future, update this file with relevant development instructions.
